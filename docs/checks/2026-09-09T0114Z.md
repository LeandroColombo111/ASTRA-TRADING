# Chequeo de salud - astra-demo

Referencia usada: `docs/checks/2026-09-08T2049Z.md` (chequeo anterior).

```
ESTADO: OK
Host: VM astra-demo (datasolutions-493815 / us-central1-a)
Snapshot generado: 2026-09-09T01:12:51.422332+00:00 (antigüedad: ~1m56s respecto de las 2026-09-09T01:14:47Z)
Heartbeat: 2026-09-09 01:12:39.402272+00:00 vs snapshot 01:12:51.422332+00:00 -> ~12s de retraso (fresco)
Velas procesadas desde el último chequeo: 5 (20:00:00 -> 01:00:00), esperadas ~4-5 (transcurrieron ~4h44m entre snapshots)
Operaciones: 0 (sin operaciones, comportamiento esperado; ~2 trades/mes es la cadencia normal)
Errores: 0 (events_total=0, events_by_kind={}, recent_events=[])
Equity demo: 74944.44869563001 vs línea base 74944.44869563001 (sin cambio; PnL de papel, no validación de estrategia)
Freno por drawdown: halted=false -- distancia: 25.0%
Rendimiento (Modified Dietz, demo, no valida la estrategia): 0.0% (net_transfers=0)
Acción requerida: ninguna urgente. Ver observación sobre reinicio del contenedor abajo -- opcional revisar logs completos en la VM si se quiere conocer la causa.
```

## Observación: el contenedor fue recreado entre chequeos

- Chequeo anterior (2026-09-08T2049Z.md): `docker_ps` mostraba `astra-demo\tUp 2 hours (healthy)\t2026-09-08 18:34:50 +0000 UTC` (mismo contenedor desde el arranque inicial documentado en `RUNTIME-BASELINE.json`).
- Chequeo actual: `docker_ps` muestra `astra-demo\tUp 3 hours (healthy)\t2026-09-08 22:16:07 +0000 UTC` -- es decir, en algún momento entre las 20:28:46Z y las 22:16:07Z se recreó el contenedor (nueva marca `CreatedAt`).
- No hay evidencia de la causa en este snapshot: `logs_tail` usa `docker logs --since 90m`, y la recreación ocurrió hace ~2h56m respecto de este snapshot, fuera de esa ventana. `events_total=0` tampoco aporta nada (no hubo eventos de negocio en el período).
- El estado persistido en el volumen (`fingerprint`, `inception_equity`, `peak`, `halted`) es consistente con el arranque original, así que no parece haber pérdida de estado -- ver `state.kv` abajo.
- Desde la recreación, el contenedor lleva ~3 horas "Up ... (healthy)" de forma continua, el heartbeat está fresco y `last_closed` avanzó a un ritmo consistente con el tiempo transcurrido (sin atasco). No hay señales de que el servicio esté actualmente degradado.
- No se tomó ninguna acción (no se reinició nada, no se investigó por SSH/gcloud). Si se quiere conocer la causa exacta (p. ej. si fue el ciclo de reintentos de compose tras 3 fallos consecutivos mencionado en `RUNTIME-BASELINE.json`, similar al incidente de auth intermitente de OKX del 2026-09-08, o un reinicio manual/de la VM), haría falta revisar los logs completos del host directamente en la VM, ya que quedaron fuera de la ventana capturada por este snapshot.

## Detalle

- **docker_ps**: `astra-demo\tUp 3 hours (healthy)\t2026-09-08 22:16:07 +0000 UTC`.
- **health**: `healthy\nEXIT:0`.
- **state.kv**: `fingerprint` igual al esperado, `inception_equity=74944.44869563001`, `peak=74944.44869563001`, `halted=false`, `last_closed="2026-09-09 01:00:00+00:00"`, `heartbeat="2026-09-09 01:12:39.402272+00:00"`.
- **okx**: `positions=[]`, `pending=[]`, `algos=[]` -- consistente con "sin operaciones".
- **performance**: `drawdown_pct=0.0`, `max_drawdown_pct=25.0`, `distance_to_halt_pct=25.0`, `halted=false`, `net_transfers=0`, `time_weighted_return_pct=0.0`.
- **logs_tail**: vacío (ventana de `--since 90m` no cubre el reinicio del contenedor, que ocurrió ~2h56m antes de este snapshot); sin líneas de "Service cycle failed" dentro de la ventana capturada.

Próximo chequeo debe comparar contra este archivo.
