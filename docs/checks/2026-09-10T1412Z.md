# Chequeo de salud - astra-demo

Referencia usada: `docs/checks/2026-09-09T1412Z.md` (chequeo anterior, ATENCION por snapshot viejo) y, para el último dato realmente fresco de `last_closed`, `docs/checks/2026-09-09T0114Z.md`.

```
ESTADO: OK
Host: VM astra-demo (datasolutions-493815 / us-central1-a)
Snapshot generado: 2026-09-10T14:01:02.092137+00:00 (antigüedad: ~11m respecto de las 2026-09-10T14:12:26Z)
Heartbeat: 2026-09-10 14:00:43.392679+00:00 vs snapshot 14:01:02.092137+00:00 -> ~19s de retraso (fresco)
Velas procesadas desde el último dato fresco: 37 (01:00:00 09/09 -> 14:00:00 10/09), esperadas ~37 (37h transcurridas, velas horarias) -- avance normal, sin atasco
Operaciones: 0 (sin operaciones, comportamiento esperado; ~2 trades/mes es la cadencia normal)
Errores: 1 transitorio, autorecuperado (ver detalle abajo)
Equity demo: 74944.44869563001 vs línea base 74944.44869563001 (sin cambio; PnL de papel, no validación de estrategia)
Freno por drawdown: halted=false -- distancia: 25.0%
Rendimiento (Modified Dietz, demo, no valida la estrategia): 0.0% (net_transfers=0)
Acción requerida: ninguna urgente. Ver observaciones sobre el error transitorio y la recreación del contenedor abajo.
```

## El pipeline de publicación volvió a funcionar

El chequeo anterior (2026-09-09T1412Z) había quedado en ATENCION porque el timer de la VM llevaba ~13h sin publicar. Este snapshot es de hace ~11 minutos, así que el timer está corriendo con normalidad de nuevo. No se puede saber desde este repo qué pasó durante el corte (no hay logs de esa ventana), pero el estado persistido (`fingerprint`, `inception_equity`, `peak`) es continuo con la línea base, y no hay eventos ni posiciones que sugieran algo anómalo haya ocurrido mientras tanto.

## Un ciclo falló y se autorecuperó (diseño esperado, no requiere acción)

`logs_tail` trae una sola línea (ventana corta desde la última publicación):

```
2026-09-10 13:57:07,826 ERROR Service cycle failed (ExchangeError); entries blocked for this cycle
```

- Esa línea la emite el loop principal en `src/astra/service.py:256`, dentro del `try/except` de `run()` (líneas 251-259): cada excepción no manejada en `svc.tick()` incrementa un contador `failures` y se loguea, pero el proceso **no aborta** salvo que se acumulen 3 fallos consecutivos (`service.py:257`, `if once or failures>=3:raise`). Si el ciclo siguiente tiene éxito, `failures` vuelve a 0 (`service.py:253`).
- El log solo registra `type(exc).__name__`, no el mensaje ni el traceback, así que no se puede identificar con certeza cuál de los `raise ExchangeError(...)` de `service.py` (por ejemplo líneas 98, 102, 114, 121, 133/135, 174, 190/193/199, 206, 226, 230) o del cliente HTTP en `src/astra/okx.py` (líneas 59, 66, 71, 73, 77, 83/86, 96, 111) fue la causa puntual.
- Lo que sí se puede afirmar con el resto del snapshot: no hubo evento de negocio asociado (`events_total=0`, `recent_events=[]`), no quedó `halted=true`, no hay posición ni protección faltante, y el heartbeat/`last_closed` del snapshot (14:00:43 / 14:00:00) son **posteriores** al error (13:57:07) y están frescos -- es decir, el ciclo siguiente al del error se completó con normalidad. Esto es consistente con el patrón de fallo transitorio de OKX (HTTP intermitente / rate limit) ya documentado en `docs/RUNTIME-BASELINE.json` para el incidente del 2026-09-08, y con el diseño: el servicio está pensado para tolerar hasta 2 fallos consecutivos sin detenerse (`service.py:257`).
- No se tomó ninguna acción sobre esto (no se reinició nada, no se investigó por SSH/gcloud). Si se repite en ráfagas o llega a 3 consecutivos, el proceso se detendría solo y el próximo chequeo lo mostraría como CAÍDO -- ahí sí habría que revisar el log completo en la VM para identificar la causa exacta antes de reiniciar manualmente.

## Observación: el contenedor volvió a recrearse

- `docker_ps` actual: `astra-demo\tUp 21 hours (healthy)\t2026-09-09 17:19:31 +0000 UTC`.
- El chequeo de 2026-09-09T0114Z (último dato fresco previo a este) tenía `CreatedAt=2026-09-08 22:16:07 +0000 UTC`. O sea que entre ese chequeo y este, el contenedor se recreó de nuevo (nueva marca de creación), y ahora lleva 21h corriendo de forma continua y saludable.
- El estado persistido en el volumen (`inception_equity=74944.44869563001`, `peak=74944.44869563001`, `halted=false`) es idéntico al de la línea base, así que no hubo pérdida de estado por la recreación.
- Como en el chequeo anterior donde se observó esto, no hay forma de determinar la causa exacta desde este repo (podría ser el ciclo de reintentos de compose tras fallos consecutivos, o un reinicio de la VM/Docker). No se investigó por SSH/gcloud, fuera del alcance de este chequeo de solo lectura.

## Detalle del snapshot

- **docker_ps**: `astra-demo\tUp 21 hours (healthy)\t2026-09-09 17:19:31 +0000 UTC`.
- **health**: `healthy\nEXIT:0`.
- **state.kv**: `fingerprint` presente y consistente, `inception_equity=74944.44869563001`, `peak=74944.44869563001`, `halted=false`, `last_closed="2026-09-10 14:00:00+00:00"`, `heartbeat="2026-09-10 14:00:43.392679+00:00"`.
- **okx**: `equity=74944.44869563001`, `positions=[]`, `pending=[]`, `algos=[]` -- consistente con "sin operaciones".
- **performance**: `drawdown_pct=0.0`, `max_drawdown_pct=25.0`, `distance_to_halt_pct=25.0`, `halted=false`, `net_transfers=0`, `time_weighted_return_pct=0.0`, `period_days=1.655` (nota propia del campo: aproximación Modified Dietz, PnL de papel, no valida la estrategia).
- **logs_tail**: 98 caracteres, una sola línea de error (ver arriba). Es solo la ventana corta desde la última publicación, no el historial completo.

Próximo chequeo debe comparar contra este archivo.
