# Chequeo de salud - astra-demo

Referencia usada: `docs/checks/2026-09-09T0114Z.md` (chequeo anterior).

```
ESTADO: ATENCION
Host: VM astra-demo (datasolutions-493815 / us-central1-a)
Snapshot generado: 2026-09-09T01:12:51.422332+00:00 (antigüedad: ~12h59m respecto de las 2026-09-09T14:12:36Z)
Heartbeat: 2026-09-09 01:12:39.402272+00:00 vs snapshot 01:12:51.422332+00:00 -> ~12s de retraso (fresco EN EL MOMENTO de ese snapshot; sin dato más reciente)
Velas procesadas desde el último chequeo: 0 nuevas visibles (mismo snapshot que el chequeo anterior; no hay dato posterior a last_closed=2026-09-09 01:00:00+00:00)
Operaciones: 0 (sin datos nuevos; nada distinto del chequeo anterior)
Errores: 0 en el contenido del snapshot, pero ver causa de ATENCION abajo
Equity demo: 74944.44869563001 vs línea base 74944.44869563001 (sin cambio; mismo dato que el chequeo anterior, PnL de papel, no validación)
Freno por drawdown: halted=false -- distancia: 25.0% (dato de hace ~13h)
Rendimiento (Modified Dietz, demo, no valida la estrategia): 0.0% (net_transfers=0, dato de hace ~13h)
Acción requerida: revisar manualmente en la VM el timer systemd de `ops/publish_status.sh` (o el propio script) -- dejó de publicar snapshots nuevos hace ~13 horas. No indica necesariamente que el bot esté caído, solo que el PIPELINE DE PUBLICACIÓN no está corriendo o está fallando antes del commit/push.
```

## Causa de ATENCION: el timer de publicación dejó de actualizar el snapshot

- `docs/status/latest.json` tiene `generated_at_utc = 2026-09-09T01:12:51.422332+00:00`. La hora de este chequeo es `2026-09-09T14:12:36Z`, es decir el snapshot tiene **~13 horas de antigüedad**, muy por encima del umbral de ~45 minutos esperado (el timer publica cada 30 min).
- Confirmado también por git log del archivo: el último commit que tocó `docs/status/latest.json` es `b8817ec` ("status: 2026-09-09T01:12:51Z"), y no hay commits posteriores sobre ese path hasta el momento de este chequeo (`git log --oneline -- docs/status/latest.json` no muestra nada más nuevo). El repo local está al día con `origin/main` (se hizo `git pull` antes de leer el archivo).
- Por diseño (`ops/publish_status.sh`), el timer hace `git fetch` + `git reset --hard origin/main`, junta el diagnóstico vía `docker exec` sobre el contenedor `astra-demo`, y solo commitea/pushea si el contenido cambió. Que no haya habido ningún commit nuevo en 13h es consistente con: (a) el timer systemd dejó de dispararse en la VM, (b) el script está fallando antes de llegar al `git add`/commit (por ejemplo, si `docker exec` empieza a fallar porque el contenedor se cayó, el script sigue por el `|| echo '{}'` de cada paso así que normalmente igual llegaría a publicar algo -- lo cual hace menos probable esta causa y más probable la (a)), o (c) la VM en sí está apagada/sin red.
- **No se puede distinguir desde este repo** si el bot (contenedor `astra-demo`) sigue vivo y operando normalmente mientras solo falla la publicación, o si ambos (bot y timer) están caídos -- son fallas independientes por diseño. Los últimos datos que sí tenemos (de hace 13h) mostraban todo sano: contenedor `Up 3 hours (healthy)`, heartbeat fresco, `last_closed` avanzando al ritmo esperado, sin errores, sin posiciones abiertas.
- Conforme a las reglas de esta tarea, no se intentó SSH, gcloud ni ningún otro camino hacia la VM. Tampoco se modificó `docs/status/latest.json` (lo escribe únicamente el timer).

## Detalle del snapshot disponible (sin cambios respecto del chequeo anterior, dato de hace ~13h)

- **docker_ps**: `astra-demo\tUp 3 hours (healthy)\t2026-09-08 22:16:07 +0000 UTC` (tal cual venía del chequeo anterior).
- **health**: `healthy\nEXIT:0`.
- **state.kv**: `halted=false`, `last_closed="2026-09-09 01:00:00+00:00"`, `heartbeat="2026-09-09 01:12:39.402272+00:00"`, sin cambios.
- **okx**: `positions=[]`, `pending=[]`, `algos=[]`.
- **performance**: `drawdown_pct=0.0`, `distance_to_halt_pct=25.0%`, `halted=false`, `net_transfers=0`, `time_weighted_return_pct=0.0%`.
- **logs_tail**: vacío (mismo dato del snapshot anterior).

## Acción requerida (sugerida, no ejecutada)

Revisar manualmente en la VM (`astra-demo`, `datasolutions-493815/us-central1-a`) el estado del timer/servicio systemd que corre `ops/publish_status.sh`, por ejemplo con `systemctl status <timer>` y `journalctl -u <servicio>` para ver por qué dejó de correr o por qué está fallando antes del push. Esto es independiente de revisar si el contenedor Docker del bot sigue arriba. No se realizó ninguna de estas acciones desde este chequeo por estar fuera del alcance de solo lectura y sin acceso a la VM.

Próximo chequeo: si el snapshot sigue sin actualizarse, mantener ESTADO: ATENCION con la misma causa. Si vuelve a publicar, comparar `last_closed` y eventos contra este archivo y contra la línea base para ver si el bot procesó velas con normalidad durante el corte de publicación.
