# Chequeo de salud - [gcp-instance-redacted]

Referencia usada: `docs/checks/2026-09-14T1349Z.md` (chequeo anterior, OK, `last_closed=2026-09-14 13:00:00+00:00`, heartbeat fresco a las 13:49:36 UTC, `docker_ps` CreatedAt `2026-09-09 17:19:31 +0000 UTC`, sin posiciones abiertas). Snapshot usado para este chequeo: el más reciente disponible en `docs/status/latest.json` al momento de escribir, `generated_at_utc=2026-09-15T14:11:08Z` (idéntico en contenido al snapshot previo de 13:41:07Z salvo el timestamp, confirmando que el estado caído es persistente y no un parpadeo de una sola publicación).

```
ESTADO: CAIDO
Host: VM de demo (ver docs/RUNTIME-BASELINE.json)
Snapshot generado: 2026-09-15T14:11:08Z (antigüedad: ~minutos respecto de ahora) -- el timer de publicación en la VM sigue corriendo con normalidad (dos publicaciones consecutivas, 13:41:07Z y 14:11:08Z, ambas con el mismo contenido caído)
Heartbeat: no verificable -- state vacío en el snapshot (docker exec falla contra un contenedor detenido)
Velas procesadas desde el último chequeo: no verificable (mismo motivo; sin acceso al sqlite de estado)
Operaciones: no verificable directamente -- "okx" vacío en el snapshot (requiere el contenedor corriendo). Último dato confirmado: 2026-09-14T1349Z (hace ~24h) sin posiciones, pending ni algos abiertos.
Errores: contenedor "astra-demo" en estado Exited(1) hace ~22h al momento del primer snapshot de este chequeo (~2026-09-14 15:41 UTC aprox.), sin recreación (mismo CreatedAt 2026-09-09 17:19:31 UTC). Confirmado persistente en dos publicaciones consecutivas 30min aparte.
Equity demo: no verificable (okx vacío)
Freno por drawdown: no verificable (performance vacío)
Rendimiento (Modified Dietz, demo, no valida la estrategia): no verificable (performance vacío)
Acción requerida: reiniciar manualmente el contenedor astra-demo en la VM, previa verificación manual en OKX demo de que no quedó posición sin protección (ver detalle abajo). No lo hice yo -- chequeo de solo lectura.
```

## Detalle del snapshot

- **generated_at_utc**: `2026-09-15T13:41:07.555511+00:00`. Fresco (~30min de antigüedad respecto de ahora), o sea el timer systemd de publicación en la VM sigue andando normalmente -- el problema es específico del contenedor `astra-demo`, no de la VM ni del pipeline de publicación.
- **docker_ps**: `astra-demo\tExited (1) 22 hours ago\t2026-09-09 17:19:31 +0000 UTC`. El contenedor existe (no fue borrado) pero terminó con código de salida 1 y no se recreó ni reinició. `CreatedAt` sin cambios respecto del chequeo anterior confirma que nadie lo recreó manualmente.
- **health**, **state**, **okx**, **performance**: todos vacíos (`""` o `{}`). Es el comportamiento esperado de `ops/publish_status.sh` cuando el contenedor no está corriendo: los `docker exec astra-demo ...` fallan (no hay proceso al cual conectarse) y el script cae a los fallback (`""`/`'{}'`) para esos cuatro campos. Esto es un síntoma más del "contenedor caído", no una falla distinta del pipeline de publicación.
- **logs_tail**: vacío. `ops/publish_status.sh` línea 42 usa `docker logs astra-demo --since 90m`, una ventana rodante de los últimos 90 minutos *respecto del momento de la publicación* -- no el historial completo del contenedor. Como el contenedor lleva ~22h detenido, la excepción que lo tumbó quedó muy por fuera de esa ventana de 90min y no está disponible en este snapshot. **No se pudo identificar la excepción puntual ni la guarda exacta de `service.py` que se disparó** -- esa información solo existe en `docker logs astra-demo` (historial completo) corrido directamente en la VM, algo que este chequeo no puede hacer (sin acceso SSH/HTTP al host).

### Guarda que probablemente aplica (sin poder confirmar el motivo raíz)

`src/astra/service.py`, función `run()` (líneas 241-261): el loop principal cuenta fallos consecutivos de `svc.tick()` y aborta el proceso cuando llega a 3:

```
253:                svc.tick();failures=0
254:            except Exception as exc:
255:                failures+=1
256:                log.error('Service cycle failed (%s); entries blocked for this cycle',type(exc).__name__)
257:                if once or failures>=3:raise
```

El `raise` en la línea 257 se propaga fuera de `run()`, el proceso termina con código de error y el contenedor queda en `Exited(1)` -- consistente con lo observado y con la advertencia ya documentada en `docs/RUNTIME-BASELINE.json` ("el loop aborta tras 3 ciclos fallidos consecutivos... Puede quedar detenido sin aviso"). Lo que **no se puede saber sin los logs completos** es cuál `Exception` concreta disparó los 3 fallos: candidatas dentro de `tick()`/`market()`/`reconcile_intent()` incluyen, por ejemplo, `ExchangeError` en líneas 114 (warmup insuficiente), 121 (gap en candles), 133/135 (intent sin reconciliar), 174/190/193/199/206/226/230 (inconsistencias de posición/orden contra OKX), o un error de red/autenticación ajeno a estas guardas (como el 401 intermitente de OKX documentado en `previous_runs` de la línea base). Cualquiera de estos exigiría revisar los logs completos en la VM antes de reiniciar.

### Ventana temporal del crash

El último chequeo confirmado bueno (`2026-09-14T1349Z.md`) registró heartbeat fresco a las **2026-09-14 13:49:36 UTC**, sin posiciones ni errores. El `docker_ps` de este snapshot marca `Exited (1) 22 hours ago` respecto de las 13:41:07 UTC del 15/09, es decir, una caída aproximada alrededor de las **2026-09-14 ~15:41 UTC** (string aproximado de Docker, no exacto). Eso ubica el crash dentro de las ~2h siguientes al último chequeo sano -- consistente con 3 ciclos fallidos consecutivos (el loop reintenta cada 10s entre ciclos, `stop.wait(10)` en línea 259, así que 3 fallos se acumulan en minutos, no en horas; la demora hasta el próximo chequeo es solo el intervalo entre publicaciones).

### Riesgo pendiente de verificar manualmente (no lo hice, es de solo lectura)

Como el snapshot no pudo leer `okx.positions`/`okx.algos` en vivo (el contenedor está caído), **no hay confirmación automática de que la cuenta demo haya quedado flat**. El último dato confirmado (chequeo de hace ~24h) no mostraba posición abierta, y el propio código intenta cerrar y halt ante protección faltante (línea 196-199) antes de fallar en la mayoría de los caminos, pero no es una garantía para todas las excepciones posibles (p.ej. una falla de red en medio de una reconciliación). Antes de reiniciar el contenedor, alguien con acceso a la VM/OKX debería confirmar manualmente el estado de la cuenta demo (posiciones, pending, algos) -- no es algo que este chequeo de solo lectura pueda o deba hacer.

### Acción requerida (para quien tenga acceso a la VM, no para este chequeo)

1. Revisar `docker logs astra-demo` (historial completo, no solo los últimos 90min) para identificar la excepción exacta que disparó los 3 fallos consecutivos.
2. Confirmar en OKX (cuenta demo) que no quedó posición sin protección.
3. Recién entonces, reiniciar manualmente el contenedor (`docker compose up -d` o equivalente) y confirmar con `astra doctor`/`astra health` que vuelve a autenticar y queda sano.

Ninguna de estas acciones se ejecutó desde este chequeo -- es un chequeo de solo lectura.
